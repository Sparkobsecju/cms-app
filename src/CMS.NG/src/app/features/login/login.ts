import { Component } from '@angular/core';

/**
 * Placeholder Login page (登入). The error interceptor redirects here after a 401.
 * A full Lab 05 sign-in form can replace this shell.
 */
@Component({
  selector: 'app-login',
  imports: [],
  templateUrl: './login.html',
  styleUrl: './login.scss',
})
export class Login {}
